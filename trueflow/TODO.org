#+title: TODO

* Highest Leverage / Critical
** Diff Relevance / Noise Filtering
   - [ ] **Generalize trivial delimiter-only diff skipping**
     - Current behavior only skips addition-only closing-brace lines.
     - Decide and encode exact delimiter set (e.g. `}`, `};`, `},`) and whether removals are included.
     - Add focused tests to avoid suppressing real logic changes.
   - [ ] **Skip paired formatting-only line replacements**
     - If a `-line`/`+line` pair differs only by insignificant formatting, treat it as unchanged for block selection.
     - Start with a conservative normalization policy; make aggressive behavior opt-in.
     - Add tests for indentation-only and trailing-whitespace-only replacements.
   - [ ] **Handle EOF newline and line-ending-only churn as non-reviewable**
     - Ensure CRLF/LF and missing-final-newline churn does not produce reviewable blocks by itself.
     - Add fixture-based regression coverage for cross-platform newline behavior.
   - [ ] **Add config knobs for diff noise filtering strictness**
     - Introduce explicit config mode(s), e.g. `strict` vs `aggressive`, for what is suppressed.
     - Keep default conservative and document tradeoffs.
   - [ ] **Add E2E fixture repo for diff-noise edge cases**
     - Build commit/revision fixtures covering whitespace-only, delimiter-only, and formatting-only changes.
     - Verify both CLI review output and TUI diff view behavior.

** Config Completion
   - [ ] **Add `scan.ignore_globs` to `trueflow.toml`**
     - Goal: allow user-defined ignore patterns beyond `.gitignore`.
   - [ ] **Add `hash.normalization` configurability**
     - Goal: expose normalization behavior as explicit config flags where it is safe to do so.

** Test/Parsing Robustness
   - [ ] **Refactor test detection query logic in `block_splitter.rs`**
     - Current state: language-specific `collect_*_test_ranges` logic is repetitive.
     - Goal: consolidate around shared `Span` overlap primitives (`src/block.rs`).
     - Keep `tests/e2e_languages.rs` (`test_all_languages_test_blocks`) green.

* Medium Priority
** Platform Verification
   - [ ] **Run explicit macOS/Linux path/newline verification matrix**
     - Existing path/subdir regressions are covered, but we still need a deliberate cross-platform pass.

** Example Repo Coverage
   - [ ] **Add smoke test for `example_repos/all_languages` review block generation**

* Low Priority / Product Direction
** Filtering Architecture Follow-up
   - [ ] **Decide if additional presentation-layer filtering is needed**
     - Current state: filtering occurs in review collection (core logic), not in TUI presentation.
     - Clarify desired architecture before changing behavior.

* Future Ideas
  - Deep semantic diffs (e.g. move detection, grouped change objects) that can map one user approval to multiple underlying deterministic change records.
